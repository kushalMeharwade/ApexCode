<UserControl x:Class="AiAssistant.UI.Controls.ConfirmationOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AiAssistant.UI.Controls"
             Focusable="True"
             IsVisibleChanged="ConfirmationOverlay_IsVisibleChanged"
             KeyDown="ConfirmationOverlay_KeyDown">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AiAssistant.UI;component/Themes/ControlStyles.xaml" />
                <ResourceDictionary Source="pack://application:,,,/AiAssistant.UI;component/Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Scrim + centered card, matching the DiffViewerOverlay pattern used in ChatPanel.xaml -->
    <Border Background="#A0000000">
        <Border x:Name="Card"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource MediumRadius}"
                MaxWidth="360"
                Margin="32"
                Padding="20,16"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.35" Color="Black" />
            </Border.Effect>
            <StackPanel>
                <TextBlock x:Name="TitleText"
                           FontWeight="Bold"
                           FontSize="{DynamicResource AppFontSizeOverride}"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8" />
                <TextBlock x:Name="MessageText"
                           FontSize="{DynamicResource InputFontSizeOverride}"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16" />
                <StackPanel x:Name="ButtonsPanel" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Content="Cancel"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="CancelButton_Click"
                            IsCancel="True" />
                    <Button x:Name="ConfirmButton" Content="OK"
                            Style="{StaticResource PrimaryButton}"
                            Click="ConfirmButton_Click"
                            IsDefault="True" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Border>
</UserControl>
