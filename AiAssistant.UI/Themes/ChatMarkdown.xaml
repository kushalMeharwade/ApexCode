<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:AiAssistant.UI.Controls"
                    xmlns:md="clr-namespace:Wpf.Ui.Markdown;assembly=Wpf.Ui.Markdown">
    <!-- Scope package styles to the message viewer, never to the Visual Studio host. -->
    <Style x:Key="ChatMarkdownViewerStyle" TargetType="local:ChatMarkdownViewer">
        <Style.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="pack://application:,,,/Wpf.Ui.Markdown;component/MarkdownViewer.xaml" />
                </ResourceDictionary.MergedDictionaries>
                <Style x:Key="{x:Static md:Styles.DocumentStyleKey}" TargetType="FlowDocument">
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                    <Setter Property="FontFamily" Value="{DynamicResource AppFont}" />
                    <Setter Property="FontSize" Value="{DynamicResource MessageFontSizeOverride}" />
                    <Setter Property="TextAlignment" Value="Left" />
                    <Setter Property="PagePadding" Value="0" />
                </Style>
                <Style TargetType="Paragraph">
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="TextAlignment" Value="Left" />
                </Style>
                <Style TargetType="List">
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Padding" Value="24,0,0,0" />
                    <Setter Property="TextAlignment" Value="Left" />
                </Style>
                <Style x:Key="{x:Static md:Styles.CodeStyleKey}" TargetType="Run">
                    <Setter Property="FontFamily" Value="{DynamicResource CodeFont}" />
                    <Setter Property="FontSize" Value="{DynamicResource CodeFontSizeOverride}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                    <Setter Property="Background" Value="{DynamicResource SurfaceColorBrush}" />
                </Style>
                <Style x:Key="{x:Static md:Styles.CodeBlockStyleKey}" TargetType="Paragraph">
                    <Setter Property="FontFamily" Value="{DynamicResource CodeFont}" />
                    <Setter Property="FontSize" Value="{DynamicResource CodeFontSizeOverride}" />
                    <Setter Property="Background" Value="{DynamicResource SurfaceColorBrush}" />
                    <Setter Property="Padding" Value="8" />
                    <Setter Property="TextAlignment" Value="Left" />
                </Style>
                <Style x:Key="ChatMarkdownHeading" TargetType="Paragraph">
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontFamily" Value="{DynamicResource AppFont}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                    <Setter Property="FontSize" Value="{DynamicResource MessageFontSizeOverride}" />
                    <Setter Property="Margin" Value="0,12,0,8" />
                    <Setter Property="TextAlignment" Value="Left" />
                </Style>
                <Style x:Key="{x:Static md:Styles.Heading1StyleKey}" TargetType="Paragraph" BasedOn="{StaticResource ChatMarkdownHeading}" />
                <Style x:Key="{x:Static md:Styles.Heading2StyleKey}" TargetType="Paragraph" BasedOn="{StaticResource ChatMarkdownHeading}" />
                <Style x:Key="{x:Static md:Styles.Heading3StyleKey}" TargetType="Paragraph" BasedOn="{StaticResource ChatMarkdownHeading}" />
                <Style x:Key="{x:Static md:Styles.Heading4StyleKey}" TargetType="Paragraph" BasedOn="{StaticResource ChatMarkdownHeading}" />
                <Style x:Key="{x:Static md:Styles.Heading5StyleKey}" TargetType="Paragraph" BasedOn="{StaticResource ChatMarkdownHeading}" />
                <Style x:Key="{x:Static md:Styles.Heading6StyleKey}" TargetType="Paragraph" BasedOn="{StaticResource ChatMarkdownHeading}" />
                <Style x:Key="{x:Static md:Styles.HyperlinkStyleKey}" TargetType="Hyperlink">
                    <Setter Property="Foreground" Value="{DynamicResource AccentPrimaryBrush}" />
                    <Setter Property="TextDecorations" Value="Underline" />
                </Style>
                <Style x:Key="{x:Static md:Styles.QuoteBlockStyleKey}" TargetType="Section">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
                    <Setter Property="BorderThickness" Value="3,0,0,0" />
                    <Setter Property="Padding" Value="10,0,0,0" />
                </Style>
                <Style x:Key="{x:Static md:Styles.TableStyleKey}" TargetType="Table">
                    <Setter Property="CellSpacing" Value="0" />
                </Style>
                <Style x:Key="{x:Static md:Styles.TableCellStyleKey}" TargetType="TableCell">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
                    <Setter Property="BorderThickness" Value="0,0,1,1" />
                    <Setter Property="Padding" Value="6" />
                </Style>
                <Style x:Key="{x:Static md:Styles.TaskListStyleKey}" TargetType="CheckBox">
                    <Setter Property="IsHitTestVisible" Value="False" />
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
                <Style x:Key="{x:Static md:Styles.ThematicBreakStyleKey}" TargetType="Line">
                    <Setter Property="Stretch" Value="Fill" />
                    <Setter Property="Stroke" Value="{DynamicResource BorderSubtleBrush}" />
                </Style>
                <Style x:Key="{x:Static md:Styles.MarkedStyleKey}" TargetType="Span">
                    <Setter Property="Background" Value="{DynamicResource SurfaceColorBrush}" />
                </Style>
            </ResourceDictionary>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ChatMarkdownViewer">
                    <FlowDocumentScrollViewer Document="{TemplateBinding Document}"
                                              Background="Transparent" IsToolBarVisible="False"
                                              VerticalScrollBarVisibility="Disabled"
                                              HorizontalScrollBarVisibility="Disabled" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
